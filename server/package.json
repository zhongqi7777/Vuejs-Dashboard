{
  "name": "node-server",
  "version": "1.0.0",
  "description": "node-server",
  "author": "ylb",
  "scripts": {
    "start": "nodemon --watch mock --exec json-server --routes ./db/routes.json ./db/db.json --host 172.27.16.15"
  },
  "devDependencies": {
    "nodemon": "^1.19.0",
    "json-server": "^0.15.1"
  },
  "engines": {
    "node": ">= 6.0.0",
    "npm": ">= 3.0.0"
  }
}
